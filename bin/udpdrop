#!/bin/sh

bin="$( basename $0 )"
bindir="$( cd $( dirname $0 ) && pwd )"
libdir="$bindir/../lib"
lib="lib$bin.so"

case "$1" in
-r|--rate)
    UDP_DROP_RATE=$2
    export UDP_DROP_RATE
    shift 2
    ;;
esac

LD_PRELOAD="$libdir/$lib${LD_PRELOAD:+ $LD_PRELOAD}"
export LD_PRELOAD
exec "$@"
